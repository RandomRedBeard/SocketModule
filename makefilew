CC = cl
CFLAG = /EHsc /c
INCLUES = 
LINKS = 
DEFINES = /DBUILDING_DLL

TARGET = libsocket

OBJS = *.obj

all: $(TARGET)

$(TARGET):
    $(CC) $(CFLAG) *.cpp
    $(CC) $(DEFINES) /LD Socket.cpp ServerSocket.cpp io.cpp myPoll.cpp /Fe: $(TARGET)

clean:
    del $(OBJS) *.lib *.dll *.exp *.exe